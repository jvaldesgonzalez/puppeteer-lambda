const capitalize = require("./capitalize");
const findInArray = require("./find-in-array");
const plainArrayToJson = require("./plain-array-to-json");
const slice = require("./slice");

module.exports = { capitalize, findInArray, plainArrayToJson, slice };
